#include <stdio.h>

//using namespace std;
//

int main() {
	char begin[256];
	char end [256];
	char current[256];
	char w[256][256];

	int n,l;
	scanf("%d %d", &n, &l);
	for (int i = 0 ; i < n ; i++)  {
		scanf("%s", &w[i]);
	};

	memcpy(current, begin, 256);




}

